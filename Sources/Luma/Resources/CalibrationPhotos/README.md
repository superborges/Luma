# Calibration Photos

此目录存放评分校准参考照。ScoreCalibrator 会扫描此目录下的 `.jpg/.jpeg/.png/.heic` 文件。

**要求**：
- 建议 20–30 张风格多样的照片（风光、人像、静物、夜景等）
- 分辨率不低于 1024×768（更大也可以，会自动缩放）
- 文件名按字母排序即校准顺序

此目录下的照片会随 App bundle 打包。
如需运行时添加，可将照片放到 `~/Library/Application Support/Luma/CalibrationPhotos/`。
