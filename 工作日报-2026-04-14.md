# 工作日报 · 2026-04-14（周二）

## 概况

**All Expeditions** 从 Sheet 思路转为主窗口内**独立全屏界面**，并与 Stitch 参考稿 **`dbda147ad6c747c2916dd28a3e0c28a8.html`** 做了一轮**高保真对齐**；顺带补齐侧栏与流程上的小交互，以及工程内「改完重启再看 UI」的协作约定。

---

## 今日已完成

### All Expeditions 主流程（架构）

- **`ProjectStore`**：`isShowingAllExpeditionsGallery`、`allExpeditionsGalleryLayout`；`openAllExpeditionsGallery` / `closeAllExpeditionsGallery` 与远征库 Sheet 解耦；离开 Library 或打开远征时自动收起图库等状态联动。
- **`ContentView`**：Library 分区内在图库打开时展示 `AllExpeditionsGalleryView`，否则 `LibraryHubView`。
- **`navSectionBinding`**：已在 **Library** 分区且全屏图库打开时，**再次点侧栏「图库」** 等价于返回 Library 主页（与顶栏「Library」面包屑一致）。
- **`ProjectLibraryView` / `LibraryHubView`**：远征库仍为 Sheet；「View All」等入口进入全屏图库；默认布局与 Store 默认改为 **Carousel（`.grid`）** 以贴合参考稿。

### All Expeditions 界面（对齐 `dbda147…html`）

- **顶栏**：`h-16`、`px-8`；面包屑（Library 可点返回 + chevron + All Expeditions）；右侧 **`w-64` Smart Search**（小字号、左内边距给放大镜、`border-white/5`、聚焦 primary 环）。
- **英雄区**：高度按 **`min(716, …)`** 约束；双渐变（自下而上 + 自左向右）与稿一致；底部 **`p-12` / `max-w-2xl`**；**Current Expedition**、**text-7xl 级标题**、Dates / Total Assets、**Enter Journey** 胶囊按钮及 **hover → primary / onPrimary**。
- **玻璃统计**：`gap-12`、`p-6`、`rounded-xl`、`border-white/5`；宽屏（≥1024）显示；**Storage** 仍为占位 **—**（未接真实磁盘统计）。
- **Continue Exploring**：`py-8`、`px-12`；标题与稿一致；**左右箭头**（`p-2` + 圆形描边）；横向 **`gap-6`、`pb-8`**；**分段控件从主行移除**，**Carousel / List** 收入 **`⋯` 菜单**，避免破坏参考稿布局。
- **轮播卡片**：**4:5**、**rounded-lg（4pt）**、`border-white/5`、底栏 **8pt 日期 + 14pt 标题**、**p-5**；有 `recent-expedition-demo` 时底层 **opacity 60 → hover 100**；不可打开远征：**Archived 角标 + 灰阶 + 低不透明**；**0.5s** 与 **scale 1.02** 与稿接近。
- **Start New Expedition**：`neutral-900` 底、虚线框、**hover 主色描边 + 加号 1.1 倍**。
- **Smart Import FAB**：右下角 **`bottom-12 right-12` 等效**、白底、光晕阴影、**hover scale 1.05**，跳转 **Imports**（会收起图库）。
- **数据展示**：Hero 日期在仅有 `createdAt` 时用 **`createdAt ~ +14 天`** 拼英文区间样式（非真实行程）；轮播排序为 **新→旧**。

### 工程与协作

- **`AGENTS.md`**：补充用户偏好——**改完后完整重启应用**再看 UI（先退出再跑新构建），避免旧进程看不到变更。
- 多次 **`swift build` / `swift test`** 通过；按约定 **killall + 启动 debug 二进制** 便于验收。

---

## 待办（后续）

1. **视觉**：轮播/Hero 是否改用**每张远征独立封面**（缩略图管线）；Hero 是否绑定**当前远征**的封面而非统一 demo。
2. **数据**：**日期区间**、**Storage** 是否与 manifest / 磁盘统计对齐；**Archived** 是否与产品定义的「归档」一致（当前部分用不可读 manifest 近似）。
3. **UE**：从图库 **打开远征** 后是否默认切 **Culling**；文案中 **Library / 图库** 是否统一或本地化。
4. **侧栏**：参考 HTML 含 **256px aside**；当前侧栏在 `ContentView` 全局，若要与整页稿 1:1 可再对主栏宽度与留白做像素级核对。

---

## 建议下次开场

1. 对照浏览器打开 **`dbda147…html`** 与本机构建并排扫一遍：顶栏、英雄区、玻璃块、轮播、FAB、虚线新建卡。
2. 决定 **封面图** 与 **日期/存储** 的数据来源后，再动模型与加载逻辑。

---

*本日报由当日协作与仓库现状整理，与 `git diff` 以实际提交为准。*
